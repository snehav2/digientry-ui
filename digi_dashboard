<!DOCTYPE html>
<html lang="en">

    <head>
      <title>WELCOME TO DIGIENTRY</title>
      <title>DIGIENTRY </title>
      
      <script src="https://cdn.rawgit.com/davidshimjs/qrcodejs/gh-pages/qrcode.min.js"></script>
      <meta name="viewport" content="width=s, initial-scale=1.0" />
      <link rel="stylesheet" href="css/style.css" />

      
      
    </head>
    <body>
      <h1>Select Destination and Generate QR Code</h1>
      <form>
        <label for="destination">Select a Destination:</label>
        <select id="destination" name="destination">
          <option value="Class">Class</option>
          <option value="MP Hall">MP Hall</option>
          <option value="Canteen">Canteen</option>
          <option value="Cafe">Cafe</option>
          <option value="Others">Others</option>
        </select>
        <br><br>
        <input type="button" value="Generate QR Code" onclick="generateQRCode()">
      </form>
      <br>
      <div id="qr-code"></div>
      <script>
        function generateQRCode() {
          const destination = document.querySelector('#destination').value;
          const qrCodeDiv = document.querySelector('#qr-code');
          
          // Generate QR code
          const qrCode = new QRCode(qrCodeDiv, {
            text: `https://example.com/destination/${destination}`,
            width: 256,
            height: 256,
            colorDark : "#000000",
            colorLight : "#ffffff",
            correctLevel : QRCode.CorrectLevel.H
          });
        }
      </script>
    </body>
  </html>